import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
    container: {
        justifyContent: 'center', 
        alignItems: 'center'
    }, 
    subtitle: { textAlign: 'center' }, 
    image: { width: 350, height: 350 }, 
    loginBtn: { width: '70%', height: 50 }

})